import styled from 'styled-components';

const Wrap = styled.section`
  > header {
    padding: 4.267vw;
    display: flex;
    justify-content: space-between;
    align-items: center;
  }
`;

export default Wrap;
